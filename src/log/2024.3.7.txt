1.Bug：输入包含邮箱和密码的请求体向/login发送POST请求时，即使邮箱和密码正确，返回的user为null,
经过排查发现是UserMapper类中的findByEmailAndPassword方法无法找到对应的用户，但数据库中实际存在该用户，经过排查发现是因为数据库中的密码字段长度不够，导致无法匹配

2.功能：实现了用户登录和用户注册功能，完成了用户登录和注册的后端逻辑和前端页面

#Todo
1.在注册时添加填写密保问题，如果密码忘记可以通过密保问题找回密码
2.用户忘记密码后可以通过填写密保问题找回密码
3.用户注册和登录后，浏览器的cookie会存储生成的token，以后每次访问时请求头会携带token
并添加拦截器，设定访问/login,/register,/findPassword时不需要token，其他请求都需要token
后端会根据token判断用户是否登录，如果登录则渲染界面，如果未登录则跳转到登录界面 √

